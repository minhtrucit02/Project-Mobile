import 'package:flutter/material.dart';

class GetImageDetail extends StatelessWidget {
  const GetImageDetail({super.key, required this.productId});

  final int productId;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
